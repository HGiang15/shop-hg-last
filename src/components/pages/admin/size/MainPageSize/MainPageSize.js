import React from 'react';
import styles from './MainPageSize.module.scss';

const MainPageSize = () => {
	return <div className={styles.container}>MainPageSize</div>;
};

export default MainPageSize;
